/**
 Default option values for running commands with {@link Cmd} or {@link runCmd}.

 This is a subset of the options available in {@link CmdDesc}, and can be used to set default values for options that are not explicitly set.
 */
export class CmdDefaults
{
  /**
   The default `quiet` option value.
   */
  static quiet = false;
}
